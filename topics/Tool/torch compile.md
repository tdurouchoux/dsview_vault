---
type: Tool
---

torch.compile is a feature in PyTorch that compiles models into optimized computation graphs for improved performance. In PyTorch Geometric, torch.compile is used to enhance the efficiency of GNN training and inference by optimizing the computational graph and reducing overhead. It supports both CPU and GPU acceleration and is particularly beneficial for large-scale graph models.