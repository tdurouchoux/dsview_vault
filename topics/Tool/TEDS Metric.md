---
type: Tool
---

The Table Extraction Description Schema (TEDS) Metric is a standardized method for evaluating the structural fidelity of parsed tables. It combines GriTS (Grid Table Similarity) and TableRecordMatch to assess the accuracy of merged cells, hierarchical headers, and overall table structure. The metric supports both exact and fast implementations, with the latter leveraging JIT acceleration for large tables.