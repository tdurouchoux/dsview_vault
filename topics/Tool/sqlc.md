---
type: Tool
---

sqlc is a tool that generates type-safe Go code from SQL queries. It allows developers to write raw SQL queries while providing compile-time safety and performance benefits. sqlc bridges the gap between SQL and application code, enabling developers to leverage the full power of SQL without sacrificing type safety or incurring runtime overhead.