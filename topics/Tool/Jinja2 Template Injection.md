---
type: Tool
---

Jinja2 template injection is a type of server-side template injection (SSTI) attack where an attacker exploits weaknesses in a template engine (e.g., Jinja2 in Python) to execute arbitrary code. This occurs when user input is improperly sanitized and directly embedded into templates, allowing attackers to inject malicious payloads.