---
type: Tool
---

The DataFusion CLI (Command Line Interface) is a tool provided by Apache DataFusion for executing SQL and DataFrame queries directly from the command line. It allows users to interact with DataFusion's query engine without writing code, making it useful for quick testing, debugging, and data exploration. The CLI supports various file formats and provides a convenient way to run queries on local files or directories.