---
type: Platform
---

npm (Node Package Manager) is a package manager for JavaScript and TypeScript, widely used for distributing and installing reusable code modules. In the context of Pi, npm is used as a platform to distribute and install Pi packages, which are modular extensions or customizations for the Pi coding agent. Users can install packages globally or locally to enhance Pi's functionality.