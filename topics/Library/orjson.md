---
type: Library
---

orjson is a fast, JSON library for Python that handles more data types and is significantly faster than the standard library's json module. It is optimized for performance, offering faster serialization and deserialization of JSON data.