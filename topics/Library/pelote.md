---
type: Library
---

A Python library for converting tabular data (e.g., pandas DataFrames) into graph structures. `ipysigma` recommends using `pelote` to transform edge tables or bipartite data into graphs for visualization.