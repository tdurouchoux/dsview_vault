---
type: Library
---

Rensa is a MinHash library implemented in Rust with Python bindings, designed for efficient near-duplicate document detection using locality-sensitive hashing (LSH). It supports batch and streaming deduplication, compact signatures (R-MinHash and C-MinHash), and incremental sketch updates. Rensa is optimized for performance through SIMD, Rust batch processing, and Python integration.