---
date: '2025-01-02'
type: Library
---

Pyodide is a Python interpreter that runs in the browser, built with WebAssembly. It allows users to run Python code and packages (including scientific libraries like NumPy and Pandas) directly in the web browser, facilitating the deployment of Python-based web applications without needing a server.