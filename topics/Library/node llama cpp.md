---
type: Library
---

A Node.js library for running LLMs locally using GGUF models. It provides APIs for embedding generation, reranking, and query expansion, and is used by QMD to power its local search capabilities.