---
type: Concept
---

An embedded database is a database system that is tightly integrated with an application, running within the same process. It eliminates the need for a separate database server, reducing complexity and improving performance for applications that require local data storage. Embedded databases are particularly useful in scenarios where low latency, offline capabilities, or minimal operational overhead are critical.