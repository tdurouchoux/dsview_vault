---
type: Concept
---

Non-Parametric Instance Discrimination is a self-supervised learning method that treats each instance in the dataset as a distinct class and learns to discriminate between them. Unlike traditional classification tasks, this approach does not rely on predefined labels but instead uses the inherent structure of the data to create a pretext task. It encourages the model to learn fine-grained representations that capture subtle differences between instances.