---
type: Concept
---

Semantic Search is a technique that involves leveraging embeddings (vector representations) of data to find items that are semantically similar, rather than relying on exact keyword matches. This process includes semantic indexing, where semantic features are extracted from content to generate embedding vectors stored in a vector database. It is widely used in applications like document retrieval, image search, and recommendation systems, enabling efficient and accurate semantic file retrieval and management.