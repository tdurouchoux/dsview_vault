---
type: Concept
---

A DataFrame in Polars is a two-dimensional, immutable data structure that stores data in columns (Series). It resembles a table with rows and columns, where each column holds values of the same data type. DataFrames are the primary data structure for data manipulation in Polars.