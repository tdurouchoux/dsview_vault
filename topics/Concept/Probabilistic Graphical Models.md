---
type: Concept
---

Probabilistic graphical models (PGMs) are a class of models that represent the probabilistic relationships among a set of variables using a graph structure. Nodes in the graph represent random variables, while edges represent conditional dependencies between them. PGMs are used for tasks such as inference, learning, and reasoning under uncertainty, and they are foundational in fields like Bayesian networks and Markov random fields.