---
type: Concept
---

Naive Bayes is a probabilistic classifier based on Bayes' theorem with an assumption of independence among the features. It is widely used for text classification and spam detection. It calculates the posterior probability of each class given the input features.