---
type: Concept
---

A computing model where each request from a client to a server is treated as an independent transaction, and no client context is stored on the server between requests. Stateless processes improve scalability and robustness, as they allow applications to handle requests without relying on local storage or session state.