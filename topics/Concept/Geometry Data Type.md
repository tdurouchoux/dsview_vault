---
type: Concept
---

The geometry data type is designed for storing spatial data, such as points, polygons, and other geometric shapes. It supports spatial operations like bounding box checks and filter pushdown, which improve query performance for geo-spatial workloads. In DuckLake, geometry types can be nested in structs, lists, and maps, and are compatible with spatial extensions like DuckDB's spatial module.