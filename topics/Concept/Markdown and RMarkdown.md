---
date: '2024-12-28'
type: Concept
---

Markdown is a lightweight markup language with plain text formatting syntax, used for writing content that can be converted to HTML. RMarkdown is an R package that extends Markdown, enabling dynamic report generation in R, allowing users to create documents that include R code and its output.