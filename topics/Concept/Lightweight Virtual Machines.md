---
date: '2025-06-15'
type: Concept
---

Lightweight virtual machines (VMs) are a type of virtualization technology that allow for efficient resource usage by running multiple containers on a single hardware instance with minimal overhead. In the context of containerization, each Linux container runs in its own lightweight VM, enabling quick deployment and isolation of containerized processes. This design helps achieve sub-second startup times and provides an isolated networking environment.