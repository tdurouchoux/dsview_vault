---
date: '2024-10-18'
type: Concept
---

YAML (YAML Ain't Markup Language) is a human-readable data serialization standard that is used commonly for configuration files. In the context of data stacks, YAML facilitates declarative configurations by allowing users to define the infrastructure as code with less verbosity compared to XML or JSON, thus improving readability and maintainability of configuration data.