---
date: '2024-12-14'
type: Concept
---

The practice of maintaining a lockfile (like uv.lock) that records the exact versions of dependencies installed for a project. This ensures reproducibility and consistency, allowing projects to be set up reliably across different machines.