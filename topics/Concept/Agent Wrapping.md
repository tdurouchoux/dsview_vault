---
type: Concept
---

Agent wrapping is a technique where an AI agent (e.g., Claude Code, Cursor, or GitHub Copilot) is configured to route its inputs and outputs through a proxy or middleware like Headroom. This allows for transparent compression, logging, and optimization of the agent's interactions with LLMs without requiring changes to the agent's core functionality.