---
type: Concept
---

A GitHub Action is a workflow automation tool that allows OpenWiki to periodically update documentation for a codebase. The action can run on a schedule (e.g., daily) and uses git diffs to identify changes in the codebase. It then updates the wiki with relevant context, ensuring that the documentation remains current without manual intervention.