---
type: Concept
---

The medallion architecture is a data design pattern that organizes data into three layers based on its level of refinement: Bronze (raw data), Silver (cleaned and conformed data), and Gold (aggregated and modeled data). This approach ensures data is progressively refined and standardized for different use cases, such as analytics or machine learning.