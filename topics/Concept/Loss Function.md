---
type: Concept
---

A loss function is a method of evaluating how well a particular algorithm models the given data. It quantifies the difference between the predicted values and the actual values. Common loss functions include least squared error, logistic loss, hinge loss, and cross-entropy.