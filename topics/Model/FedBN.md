---
type: Model
---

FedBN (Federated Batch Normalization) is a federated learning strategy that addresses the challenge of batch normalization in federated settings. It allows clients to maintain their own batch normalization layers while sharing other model parameters, improving performance in heterogeneous federated environments.