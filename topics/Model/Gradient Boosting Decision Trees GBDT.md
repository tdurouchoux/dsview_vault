---
date: '2024-07-10'
type: Model
---

Gradient Boosting Decision Trees (GBDT) is a powerful ensemble technique that builds models sequentially by fitting to the residual errors of the previous models, effectively minimizing a loss function over iterations. Popular implementations include XGBoost and LightGBM, which are optimized for performance and scalability.