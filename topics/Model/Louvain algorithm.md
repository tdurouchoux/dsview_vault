---
type: Model
---

A community detection algorithm that optimizes modularity to partition nodes into communities. The algorithm is used in `ipysigma` to compute node communities directly within the widget, enabling visual differentiation of communities via color or other attributes.