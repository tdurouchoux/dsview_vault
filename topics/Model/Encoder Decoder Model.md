---
date: '2025-01-14'
type: Model
---

The encoder-decoder architecture is a framework commonly used in sequence-to-sequence tasks. The encoder processes the input sequence, creating a context vector that captures the relevant information, while the decoder generates the output sequence conditioned on this context, allowing for complex tasks such as translation or summarization.