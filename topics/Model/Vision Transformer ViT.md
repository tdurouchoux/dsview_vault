---
date: '2025-03-17'
type: Model
---

The Vision Transformer (ViT) is a deep learning model that applies transformer architectures, originally designed for sequence data, to image classification tasks by treating images as sequences of patches. ViT models images as sequences of patches, enabling them to capture global dependencies and perform well on tasks like image classification, having shown strong performance on image recognition benchmarks.