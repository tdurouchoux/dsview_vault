---
date: '2025-04-29'
type: Model
---

Graph Convolutional Networks are a type of neural network that extends the concept of convolutional networks to graph-structured data. They optimize the representation of nodes by using layers that perform convolutions over the graph structure, aggregating information from a node's local neighborhood to improve classification and recognition tasks on graph data.